/**
 * Generic active/inactive status used by reference/master-data tables
 * (industrial_zones, job_categories, recruitment_sources, company_contacts, candidates, admin_users, companies).
 */
export enum CommonStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}
